# AGENTS.md

## 앱 테스트

- 앱을 직접 실행하거나 조작하는 테스트는 반드시 사용자에게 먼저 묻습니다.
- 사용자가 Codex의 직접 테스트를 허용할지, 사용자가 직접 테스트할지 답한 뒤 진행합니다.
- 빌드, 타입 검사, 린트처럼 앱을 직접 조작하지 않는 정적 검증은 작업 범위에 맞게 실행합니다.

## Git

- 커밋하기 전에 반드시 현재 변경된 파일과 diff를 확인합니다.
- 요청과 관계없는 변경사항이 포함되지 않았는지 확인합니다.
- 사용자가 작성했거나 다른 개발자가 작업 중인 변경사항을 임의로 커밋하지 않습니다.
- 요청과 관련된 변경사항만 커밋합니다.
- 커밋 메시지는 짧은 한글로 작성하며 변경 종류에 따라 다음 접두사를 붙입니다.
  - `add:` 새로운 기능이나 파일 추가
  - `edit:` 기존 기능이나 파일 수정
  - `fix:` 버그 수정
  - `remove:` 기능이나 파일 제거
- 커밋을 생성한 경우 별도 지시가 없어도 현재 브랜치를 자동으로 push합니다.
- push 전에 원격 변경사항으로 인해 충돌 가능성이 있는지 확인합니다.
- 다른 개발자의 원격 변경사항이 존재하면 이를 덮어쓰지 않습니다.
- 필요하면 현재 브랜치와 원격 브랜치의 차이를 먼저 확인합니다.
- force push를 사용하지 않습니다.
- 사용자가 명시적으로 요청하지 않는 한 다음과 같이 다른 사람의 작업을 잃을 가능성이 있는 명령은 사용하지 않습니다.
  - `git reset --hard`
  - `git clean`
  - `git checkout -- <file>`
  - `git restore`를 이용한 기존 변경사항 제거
  - force push
  - 다른 사람의 커밋이나 작업 내용을 임의로 되돌리는 명령
- 충돌이 발생하거나 원격 변경사항 때문에 안전하게 push할 수 없다면 기존 작업을 덮어쓰지 않습니다.
